#服务器端口
server:
  port: 6100

#数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${platform.datasource.prod.url}
    username: ${platform.datasource.prod.username}
    password: ${platform.datasource.prod.password}
